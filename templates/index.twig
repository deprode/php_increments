<!doctype html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{ blog_title }}</title>
</head>
<body>
<header>
    <h1 class="title">{{ blog_title }}</h1>
    <p class="subtitle">{{ blog_subtitle }}</p>
</header>
<nav>
    <ul>
        {% for post in posts %}
            <li><a href="/article/{{ post.id }}">{{ post.title }}</a></li>
        {% endfor %}
    </ul>
    <p><a href="/article/new">投稿する</a></p>
</nav>
<footer>
    ©{{ author }}
</footer>
</body>
</html>